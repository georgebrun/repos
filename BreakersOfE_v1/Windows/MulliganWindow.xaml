<Window x:Class="BreakersOfE.Windows.MulliganWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mulligan"
        Width="820" Height="340"
        MinWidth="600" MinHeight="300"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Background="#1A1A2E">

    <Window.Resources>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
        <Style x:Key="SelectedCardStyle" TargetType="Border"
               BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="BorderBrush" Value="#FF4444"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#FF4444" BlurRadius="12"
                                      ShadowDepth="0" Opacity="0.9"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Name="TitleText"
                       Text="Select 1 card to place on the bottom of your library"
                       Foreground="White" FontSize="14" FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
            <TextBlock Name="SubtitleText"
                       Text="Click a card to select it. Click again to deselect. Right-click to view."
                       Foreground="#AAAAAA" FontSize="11"
                       HorizontalAlignment="Center" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Card display -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">
            <StackPanel Name="CardPanel" Orientation="Horizontal"
                        HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </ScrollViewer>

        <!-- Scryfall attribution -->
        <TextBlock Grid.Row="2" Text="Images provided by Scryfall" FontSize="9" Foreground="Gray" HorizontalAlignment="Center" Margin="0,2,0,0"/>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Name="SelectionStatus"
                       Text="0 of 1 selected" Foreground="#AAAAAA"
                       FontSize="12" VerticalAlignment="Center"/>

            <Button Grid.Column="1" Name="BtnMulliganAgain"
                    Content="↺ Mulligan Again"
                    Width="130" Height="32" FontSize="12"
                    Background="#333366" Foreground="White"
                    BorderBrush="#5555AA"
                    Click="BtnMulliganAgain_Click"/>

            <Button Grid.Column="3" Name="BtnKeep"
                    Content="✓ Keep Hand"
                    Width="120" Height="32" FontSize="12" FontWeight="SemiBold"
                    Background="#226622" Foreground="White"
                    BorderBrush="#44AA44"
                    IsEnabled="False"
                    Click="BtnKeep_Click"/>
        </Grid>
    </Grid>
</Window>
