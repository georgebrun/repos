<Window x:Class="BreakersOfE.Windows.KeywordDictionaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keyword Dictionary — Breakers of E"
        Width="700" Height="680"
        MinWidth="560" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search box -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Name="TxtSearch"
                     Height="28" Padding="6,0"
                     VerticalContentAlignment="Center"
                     TextChanged="TxtSearch_Changed"/>
            <TextBlock Grid.Column="1" Name="CountText"
                       Text="0 keywords" Margin="8,0,0,0"
                       VerticalAlignment="Center" Foreground="Gray"/>
        </Grid>

        <!-- Keyword grid -->
        <DataGrid Grid.Row="1" Name="DictGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionChanged="DictGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Keyword"
                    Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="Definition"
                    Binding="{Binding Definition}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="32"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Definition panel -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource BorderBrush}"
                Padding="0,8,0,0" Margin="0,8,0,0">
            <StackPanel>
                <TextBlock Name="SelectedKeywordName"
                           FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBlock Name="SelectedKeywordDef"
                           TextWrapping="Wrap" FontSize="12"
                           Foreground="{DynamicResource MutedTextBrush}"
                           MaxHeight="80"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
