<Window x:Class="BreakersOfE.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Breakers of E"
        Width="380" Height="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo / Title -->
        <TextBlock Grid.Row="0" Text="Breakers of E"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource AccentBrush}"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1" Name="VersionText"
                   FontSize="13" HorizontalAlignment="Center"
                   Foreground="Gray" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2"
                   Text="A Magic: The Gathering collection manager&#x0a;and deck builder."
                   FontSize="12" HorizontalAlignment="Center"
                   TextAlignment="Center" TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <!-- Data attribution -->
        <TextBlock Grid.Row="3"
                   Text="Card data provided by Scryfall.&#x0a;This app is not affiliated with Wizards of the Coast."
                   FontSize="10" HorizontalAlignment="Center"
                   TextAlignment="Center" TextWrapping="Wrap"
                   Foreground="Gray" VerticalAlignment="Center"/>

        <!-- Open app folder -->
        <Button Grid.Row="4" Content="📂 Open App Data Folder"
                HorizontalAlignment="Center" Padding="10,4" Margin="0,8,0,0"
                Click="BtnOpenFolder_Click"/>

        <!-- Close -->
        <Button Grid.Row="5" Content="Close" IsDefault="True" IsCancel="True"
                Width="80" Height="28" HorizontalAlignment="Center"
                Margin="0,10,0,0" Click="BtnClose_Click"/>
    </Grid>
</Window>
