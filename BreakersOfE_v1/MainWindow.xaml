<Window x:Class="BreakersOfE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Breakers of E"
        Icon="pack://application:,,,/BreakersOfE;component/Resources/Images/BreakersOfE.ico"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        KeyDown="MainWindow_KeyDown"
        Closing="MainWindow_Closing">

    <Window.Resources>

        <!-- ── Gripper template for column resizing ── -->
        <ControlTemplate x:Key="GripperTemplate" TargetType="Thumb">
            <Border Background="Transparent"/>
        </ControlTemplate>

        <!-- ── DataGrid column header with hover funnel + sort arrow ── -->
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="{DynamicResource GridHeaderBrush}"/>
            <Setter Property="Foreground"      Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding"         Value="6,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid Background="{TemplateBinding Background}"
                              SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.ColumnSpan="3"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <TextBlock Grid.Column="0"
                                       Text="{TemplateBinding Content}"
                                       Padding="{TemplateBinding Padding}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                            <Path Grid.Column="1" Name="SortArrow"
                                  Visibility="Collapsed"
                                  Fill="{DynamicResource PrimaryTextBrush}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  Width="8" Height="5" Stretch="Fill"
                                  Margin="0,0,4,0" RenderTransformOrigin="0.5,0.5">
                                <Path.Data>
                                    <PathGeometry>M 0,0 L 4,4 L 8,0 Z</PathGeometry>
                                </Path.Data>
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Path.RenderTransform>
                            </Path>
                            <Button Grid.Column="2" Name="FunnelButton"
                                    Width="18" Height="18" Margin="0,0,4,0"
                                    Padding="0" Background="Transparent"
                                    BorderThickness="0" Cursor="Hand"
                                    Visibility="Hidden" VerticalAlignment="Center"
                                    Tag="{TemplateBinding Content}"
                                    Click="ColumnFunnel_Click">
                                <TextBlock Name="FunnelIcon" Text="&#xE16E;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="11" Foreground="Gray"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                            </Button>
                            <!-- Resize grippers -->
                            <Thumb x:Name="PART_LeftHeaderGripper"
                                   Grid.ColumnSpan="3"
                                   HorizontalAlignment="Left"
                                   Width="8" Cursor="SizeWE"
                                   Template="{StaticResource GripperTemplate}"/>
                            <Thumb x:Name="PART_RightHeaderGripper"
                                   Grid.ColumnSpan="3"
                                   HorizontalAlignment="Right"
                                   Width="8" Cursor="SizeWE"
                                   Template="{StaticResource GripperTemplate}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FunnelButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Ascending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="180" CenterX="4" CenterY="2.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="SortDirection" Value="Descending">
                                <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="SortArrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="0" CenterX="4" CenterY="2.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── DataGrid row style ── -->
        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="{Binding RowBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{Binding RowForegroundBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="14"/>
            <Setter Property="MinHeight"  Value="26"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderBrush"     Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="BorderThickness" Value="0,1,0,1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ── DataGrid cell style ── -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderBrush"     Value="{Binding CellBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding"         Value="4,2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{Binding RowForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ── Summary row style ── -->
        <Style x:Key="SummaryBorderStyle" TargetType="Border">
            <Setter Property="Background"      Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
            <Setter Property="Height"          Value="30"/>
            <Setter Property="Padding"         Value="10,0"/>
        </Style>

        <!-- ── Deck tab style ── -->
        <Style x:Key="DeckTabStyle" TargetType="TabItem">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Padding"    Value="8,3"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="Padding"    Value="6,3"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="#1A1A1A"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="#1A1A1A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background"               Value="#FFFFFF"/>
            <Setter Property="Foreground"               Value="#1A1A1A"/>
            <Setter Property="BorderBrush"              Value="#CCCCCC"/>
            <Setter Property="BorderThickness"          Value="1"/>
            <Setter Property="FontFamily"               Value="Segoe UI"/>
            <Setter Property="FontSize"                 Value="13"/>
            <Setter Property="Height"                   Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ══ ROW 0 — MENU BAR ══ -->
        <Menu Grid.Row="0"
              Background="{DynamicResource SurfaceAltBrush}"
              Foreground="{DynamicResource PrimaryTextBrush}"
              FontSize="13">
            <Menu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                    <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                </Style>
            </Menu.Resources>
            <MenuItem Header="_File">
                <!-- Deck operations -->
                <MenuItem Header="_New Deck..."      Name="MenuNewDeck"      IsEnabled="False" Click="MenuNewDeck_Click"/>
                <MenuItem Header="_Open Deck..."     Name="MenuOpenDeck"     IsEnabled="False" Click="MenuOpenDeck_Click"/>
                <MenuItem Header="_Recent Decks"     Name="MenuRecentDecks">
                    <MenuItem Header="(no recent decks)" Name="MenuNoRecentDecks" IsEnabled="False"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Save Deck"        Name="MenuSaveDeck"      IsEnabled="False" Click="MenuSaveDeck_Click"/>
                <MenuItem Header="Save _All Decks"   Name="MenuSaveAllDecks"  IsEnabled="False" Click="MenuSaveAllDecks_Click"/>
                <Separator/>
                <MenuItem Header="_Close Deck"       Name="MenuCloseDeck"     IsEnabled="False" Click="BtnCloseDeck_Click"/>
                <MenuItem Header="Close _All Decks"  Name="MenuCloseAllDecks" IsEnabled="False" Click="MenuCloseAllDecks_Click"/>
                <Separator/>
                <!-- Import / Export -->
                <MenuItem Header="⬇ _Import..."  Click="MenuImport_Click"/>
                <MenuItem Header="⬆ _Export..."  Click="MenuExport_Click"/>
                <Separator/>
                <!-- App folders -->
                <MenuItem Header="📂 Open Folder">
                    <MenuItem Header="📁 Root Folder (Breakers of E)"  Click="MenuOpenFolder_Root_Click"/>
                    <Separator/>
                    <MenuItem Header="🂠 Decks"                         Click="MenuOpenFolder_Decks_Click"/>
                    <MenuItem Header="🃏 Card Images"                   Click="MenuOpenFolder_CardImages_Click"/>
                    <MenuItem Header="📤 Exports"                       Click="MenuOpenFolder_Exports_Click"/>
                    <MenuItem Header="📥 Imports"                       Click="MenuOpenFolder_Imports_Click"/>
                    <MenuItem Header="🔍 Filters"                       Click="MenuOpenFolder_Filters_Click"/>
                    <MenuItem Header="🗂 Collection"                    Click="MenuOpenFolder_Collection_Click"/>
                    <Separator/>
                    <MenuItem Header="🎮 Tabletop — Playmats"           Click="MenuOpenFolder_Playmats_Click"/>
                    <MenuItem Header="🎮 Tabletop — Sleeves"            Click="MenuOpenFolder_Sleeves_Click"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Preferences"      Click="MenuPreferences_Click"/>
                <Separator/>
                <MenuItem Header="E_xit"             Click="MenuExit_Click"/>
            </MenuItem>
            <MenuItem Header="_View" Visibility="Collapsed">
                <MenuItem Header="_Theme">
                    <MenuItem Header="_Light Mode" Click="MenuThemeLight_Click"/>
                    <MenuItem Header="_Dark Mode"  Click="MenuThemeDark_Click"/>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Update Card Database"      Click="MenuUpdateDatabase_Click"/>
                <MenuItem Header="_Update Prices Only"        Click="MenuUpdatePrices_Click"/>
                <Separator/>
                <MenuItem Header="⬇ Download Missing Card Images"
                          Name="MenuDownloadImages"
                          Click="MenuDownloadImages_Click"/>
                <Separator/>
                <MenuItem Header="🔗 Combine Duplicate Collection Rows"
                          Click="MenuCombineDuplicates_Click"
                          ToolTip="Merge multiple rows of the same card into one, adding quantities together"/>
                <MenuItem Header="📦 Migrate Collection Card Data"
                          Click="MenuMigrateCollectionData_Click"
                          ToolTip="Populates embedded card data in collection entries from the pool database (one-time upgrade)"/>
                <MenuItem Header="💰 Update Deck Prices"
                          Click="MenuUpdateDeckPrices_Click"
                          ToolTip="Updates prices in all currently open decks from the pool"/>
                <Separator/>
                <MenuItem Header="📒 _Trade Binder"       Name="MenuTradeBinder"
                          Click="MenuTradeBinder_Click"/>
                <MenuItem Header="🔍 _Keyword Search"     Name="MenuKeywordSearch"
                          Click="MenuKeywordSearch_Click"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="❓ _Help..."              Click="MenuHelp_Click"/>
                <Separator/>
                <MenuItem Header="📖 _Keyword Dictionary"  Click="MenuKeywordDictionary_Click"/>
                <Separator/>
                <MenuItem Header="🔄 Check for _Updates"   Click="MenuCheckForUpdates_Click"/>
                <Separator/>
                <MenuItem Header="🎬 Show _Splash Screen"  Click="MenuShowSplash_Click"/>
                <Separator/>
                <MenuItem Header="_About"                   Click="MenuAbout_Click"/>
            </MenuItem>
        </Menu>

        <!-- ══ ROW 1 — TOOLBAR ══ -->
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <WrapPanel Margin="4,2" Orientation="Horizontal">

                <!-- GROUP 1: DECK FILE -->
                <Button Name="BtnNewDeck"          Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE132;" ToolTip="New Deck"              IsEnabled="False" Click="BtnNewDeck_Click"         Margin="2,2"/>
                <Button Name="BtnOpenDeck"         Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE1A5;" ToolTip="Open Deck"             IsEnabled="False" Click="BtnOpenDeck_Click"        Margin="2,2"/>
                <Button Name="BtnSaveDeck"         Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE105;" ToolTip="Save Deck"             IsEnabled="False" Click="BtnSaveDeck_Click"        Margin="2,2"/>
                <Button Name="BtnSaveAllDecks"     Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE116;" ToolTip="Save All Decks"        IsEnabled="False" Click="BtnSaveAllDecks_Click"    Margin="2,2"/>
                <Button Name="BtnCloseDeck"        Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE106;" ToolTip="Close Deck"            IsEnabled="False" Click="BtnCloseDeck_Click"       Margin="2,2"/>
                <Button Name="BtnCloseAllDecks"    Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE8F4;" ToolTip="Close All Decks"       IsEnabled="False" Click="BtnCloseAllDecks_Click"   Margin="2,2"/>
                <Button Name="BtnDeckProperties"   Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE946;" ToolTip="Deck Properties"       IsEnabled="False" Click="BtnDeckProperties_Click"  Margin="2,2"/>
                <Button Name="BtnDeckLegality"     Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE73E;" ToolTip="Deck Legality"         IsEnabled="False" Click="BtnDeckLegality_Click"    Margin="2,2"/>
                <Button Name="BtnDeckStats"        Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE9F9;" ToolTip="Deck Statistics"       IsEnabled="False" Click="BtnDeckStats_Click"       Margin="2,2"/>

                <Separator Style="{DynamicResource ToolbarSeparatorStyle}" Margin="4,2"/>

                <!-- GROUP 2: DECK CARDS -->
                <Button Name="BtnAddToDeck"        Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE109;" ToolTip="Add 1 Card to Deck"              IsEnabled="False" Click="BtnAddToDeck_Click"       Margin="2,2"/>
                <Button Name="BtnAdd4ToDeck"       Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE8D3;" ToolTip="Add 4 Cards to Deck (Standard)"  IsEnabled="False" Click="BtnAdd4ToDeck_Click"      Margin="2,2"/>
                <Button Name="BtnAddFoilToDeck"    Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE734;" ToolTip="Add 1 Foil Card to Deck"         IsEnabled="False" Click="BtnAddFoilToDeck_Click"   Margin="2,2"/>
                <Button Name="BtnRemoveFromDeck"   Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE108;" ToolTip="Remove Card from Deck"           IsEnabled="False" Click="BtnRemoveFromDeck_Click"  Margin="2,2"/>
                <Button Name="BtnDeckQtyIncrease"  Style="{DynamicResource ToolbarButtonStyle}" Content="(+)" ToolTip="Increase Qty in Deck"  IsEnabled="False" Click="BtnDeckQtyIncrease_Click" Margin="2,2" FontFamily="Segoe UI" FontSize="12"/>
                <Button Name="BtnDeckQtyDecrease"  Style="{DynamicResource ToolbarButtonStyle}" Content="(-)" ToolTip="Decrease Qty in Deck"  IsEnabled="False" Click="BtnDeckQtyDecrease_Click" Margin="2,2" FontFamily="Segoe UI" FontSize="12"/>

                <Separator Style="{DynamicResource ToolbarSeparatorStyle}" Margin="4,2"/>

                <!-- GROUP 3: COLLECTION -->
                <Button Name="BtnAddToCollection"      Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE109;" ToolTip="Add 1 Card to Collection"      IsEnabled="False" Click="BtnAddToCollection_Click"      Margin="2,2"/>
                <Button Name="BtnAddFoilToCollection"  Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE734;" ToolTip="Add 1 Foil Card to Collection"  IsEnabled="False" Click="BtnAddFoilToCollection_Click"  Margin="2,2"/>
                <Button Name="BtnImportDeckToCollection" Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE1A3;" ToolTip="Import Entire Deck to Collection" IsEnabled="False" Click="BtnImportDeckToCollection_Click" Margin="2,2"/>
                <Button Name="BtnRemoveFromCollection" Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE108;" ToolTip="Remove Card from Collection"    IsEnabled="False" Click="BtnRemoveFromCollection_Click" Margin="2,2"/>

                <Separator Style="{DynamicResource ToolbarSeparatorStyle}" Margin="4,2"/>

                <!-- GROUP 4: FILTER & SEARCH -->
                <Button Name="BtnFilter"         Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE16E;" ToolTip="Filter Cards"                  Click="BtnFilter_Click"         Margin="2,2"/>
                <Button Name="BtnAdvancedFilter" Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE16F;" ToolTip="Advanced Filter"               Click="BtnAdvancedFilter_Click" Margin="2,2"/>
                <Button Name="BtnSearch"         Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE11A;" ToolTip="Search for Card"               Click="BtnSearch_Click"         Margin="2,2"/>
                <TextBox Name="SearchBox"
                         PreviewKeyDown="SearchBox_PreviewKeyDown"
                         Style="{DynamicResource BaseTextBoxStyle}"
                         Height="26" Width="200"
                         VerticalContentAlignment="Center"
                         Margin="2,2"
                         TextChanged="SearchBox_TextChanged"/>
                <Button Name="BtnFindNext"       Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE70D;" ToolTip="Find Next"                     Click="BtnFindNext_Click"       Margin="2,2"/>
                <Button Name="BtnFindPrev"       Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE70E;" ToolTip="Find Previous"                 Click="BtnFindPrev_Click"       Margin="2,2"/>
                <Button Name="BtnClearSearch"    Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE10A;" ToolTip="Cancel Incremental Search"     Click="BtnClearSearch_Click"    Margin="2,2"/>
                <Button Name="BtnToggleLegality" Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE73E;" ToolTip="Toggle Legality Columns"       Click="BtnLegality_Click"       Margin="2,2"/>
                <Button Name="BtnColumnChooser"  Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE179;" ToolTip="Show/Hide Columns"             Click="BtnColumnChooser_Click"  Margin="2,2"/>
                <Button Name="BtnKeywordSearch"  Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE11B;" ToolTip="Keyword Search"                  Click="BtnKeywordSearch_Click"  Margin="2,2"/>

                <Separator Style="{DynamicResource ToolbarSeparatorStyle}" Margin="4,2"/>

                <!-- GROUP 5: VIEW -->
                <Button Name="BtnUpdateDb" Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE117;" ToolTip="Update Card Database" Click="BtnUpdateDb_Click" Margin="2,2"/>
                <Button Name="BtnTheme"    Style="{DynamicResource ToolbarButtonStyle}" Content="&#xE793;" ToolTip="Toggle Light/Dark Mode" Click="BtnTheme_Click"  Margin="2,2" Visibility="Collapsed"/>

                <!-- Mode dropdown -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,2,4,2">
                    <TextBlock Text="Mode:" Foreground="{DynamicResource MutedTextBrush}"
                               FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox Name="ViewModeComboBox"
                              Style="{DynamicResource BaseComboBoxStyle}"
                              Width="220" Height="28"
                              SelectionChanged="ViewModeComboBox_SelectionChanged">
                        <ComboBoxItem Content="Pool → Collection"  Tag="PoolToCollection"/>
                        <ComboBoxItem Content="Pool → Planechase"  Tag="PoolToPlanechase"/>
                        <ComboBoxItem Content="Pool → Archenemy"   Tag="PoolToArchenemy"/>
                        <ComboBoxItem Content="Pool → Vanguard"    Tag="PoolToVanguard"/>
                        <ComboBoxItem Content="Pool → Conspiracy"  Tag="PoolToConspiracy"/>
                        <ComboBoxItem Content="Pool → Tokens"      Tag="PoolToTokens"/>
                        <ComboBoxItem Content="Pool → Art Series"  Tag="PoolToArtSeries"/>
                        <ComboBoxItem Content="Pool → Deck"        Tag="PoolToDeck"/>
                        <ComboBoxItem Content="Collection → Deck"  Tag="CollectionToDeck"/>
                        <ComboBoxItem Content="Deck → Collection"  Tag="DeckToCollection"/>
                        <ComboBoxItem Content="Collection → Trade Binder" Tag="CollectionToTradeBinder"/>
                        <ComboBoxItem Content="Pool → Want List"   Tag="PoolToWantList"/>
                    </ComboBox>
                </StackPanel>

            </WrapPanel>
        </Border>

        <!-- ══ ROW 2 — MAIN CONTENT ══ -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" MinWidth="180"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL -->
            <Border Grid.Column="0"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Visible">
                    <StackPanel Margin="10">
                        <Border Name="CardImageBorder"
                                MaxWidth="250" MaxHeight="348"
                                Background="{DynamicResource SurfaceAltBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" CornerRadius="4"
                                Margin="0,6,0,10" HorizontalAlignment="Center">
                            <Image Name="CardImage" Stretch="Uniform"
                                   MouseLeftButtonDown="CardImage_MouseDoubleClick"/>
                        </Border>
                        <!-- DFC flip button — hidden unless card has a back face -->
                        <Button Name="BtnFlipCard"
                                Content="🔄 Show Back Face"
                                Visibility="Collapsed"
                                Margin="0,-6,0,8"
                                Padding="6,3"
                                HorizontalAlignment="Center"
                                FontSize="11"
                                Click="BtnFlipCard_Click"/>
                        <TextBlock Text="NAME"             Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailName"       Style="{DynamicResource DetailValueStyle}" FontSize="15" FontWeight="Bold"/>
                        <TextBlock Text="MANA COST"        Style="{DynamicResource DetailLabelStyle}"/>
                        <WrapPanel Name="DetailManaCostPanel" Margin="0,2,0,0"/>
                        <TextBlock Text="TYPE"             Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailType"       Style="{DynamicResource DetailValueStyle}"/>
                        <TextBlock Text="SET"              Style="{DynamicResource DetailLabelStyle}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <Image Name="DetailSetSymbol" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Name="DetailSet" Style="{DynamicResource DetailValueStyle}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="COLLECTOR NUMBER" Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailCollectorNumber" Style="{DynamicResource DetailValueStyle}"/>
                        <TextBlock Text="RARITY"           Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailRarity"     Style="{DynamicResource DetailValueStyle}"/>
                        <TextBlock Name="DetailPTLabel"    Text="POWER / TOUGHNESS" Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailPT"         Style="{DynamicResource DetailValueStyle}"/>
                        <TextBlock Text="ORACLE TEXT"      Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailOracle"     Style="{DynamicResource DetailValueStyle}" FontSize="12"/>
                        <TextBlock Text="FLAVOR TEXT"      Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailFlavor"     Style="{DynamicResource DetailValueStyle}" FontSize="12" FontStyle="Italic" Foreground="{DynamicResource MutedTextBrush}"/>
                        <TextBlock Text="ARTIST"           Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailArtist"     Style="{DynamicResource DetailValueStyle}"/>
                        <TextBlock Text="AVAILABLE AS"     Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailFoilNonFoil" Style="{DynamicResource DetailValueStyle}"/>
                        <TextBlock Text="PRICES (USD)"     Style="{DynamicResource DetailLabelStyle}"/>
                        <TextBlock Name="DetailPrices"     Style="{DynamicResource DetailValueStyle}" FontSize="12"/>
                        <TextBlock Text="FORMAT LEGALITY"  Style="{DynamicResource DetailLabelStyle}"/>
                        <StackPanel Name="DetailLegalityPanel" Margin="0,2,0,0"/>
                        <TextBlock Text="Images provided by Scryfall"
                                   FontSize="9" Foreground="Gray"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,4"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- SPLITTER -->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"
                          Background="{DynamicResource BorderBrush}" Cursor="SizeWE"
                          Focusable="False" IsTabStop="False"/>

            <!-- RIGHT PANEL -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="120"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" MinHeight="100"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- TOP TABLE -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>

                    <!-- Top bar -->
                    <Border Grid.Row="0" Style="{DynamicResource GroupByBarStyle}">
                        <TextBlock Text="Hover a column header to filter"
                                   Foreground="{DynamicResource MutedTextBrush}"
                                   FontSize="12" VerticalAlignment="Center" Margin="6,0"/>
                    </Border>

                    <!-- Search bar -->
                    <Border Grid.Row="1"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,0,0,1" Padding="8,4">
                        <DockPanel>
                            <Button DockPanel.Dock="Left"
                                    Content="&#x2630;"
                                    ToolTip="Show/Hide Columns"
                                    Tag="Top"
                                    Width="22" Height="22"
                                    VerticalAlignment="Center"
                                    FontSize="11"
                                    Padding="0"
                                    Margin="0,0,6,0"
                                    Style="{DynamicResource BaseButtonStyle}"
                                    PreviewMouseLeftButtonDown="ChooserBtn_PreviewMouseDown"/>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                        DockPanel.Dock="Left" Margin="0,0,8,0">
                                <TextBlock Name="TopSearchLabel" Text="Pool"
                                           Foreground="{DynamicResource PrimaryTextBrush}"
                                           FontWeight="SemiBold" FontSize="14"
                                           VerticalAlignment="Center"/>
                                <StackPanel Name="TopTableManaSymbols"
                                            Orientation="Horizontal"
                                            Margin="6,0,0,0"
                                            VerticalAlignment="Center"/>
                            </StackPanel>
                            <!-- Top table zoom -->
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right"
                                        VerticalAlignment="Center" Margin="8,0,0,0">
                                <Button Content="−" Width="22" Height="22" FontSize="14"
                                        Style="{DynamicResource BaseButtonStyle}"
                                        ToolTip="Zoom Out (Top)" Padding="0"
                                        Click="BtnTopZoomOut_Click"/>
                                <TextBox Name="TopZoomInput" Text="100"
                                         Foreground="{DynamicResource MutedTextBrush}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         FontSize="11" VerticalAlignment="Center"
                                         HorizontalContentAlignment="Center"
                                         Margin="4,0" Width="32" Height="20"
                                         PreviewKeyDown="ZoomInput_PreviewKeyDown"
                                         LostFocus="TopZoomInput_LostFocus"
                                         GotFocus="ZoomInput_GotFocus"/>
                                <TextBlock Text="%" FontSize="11"
                                           Foreground="{DynamicResource MutedTextBrush}"
                                           VerticalAlignment="Center" Margin="0,0,2,0"/>
                                <Button Content="+" Width="22" Height="22" FontSize="14"
                                        Style="{DynamicResource BaseButtonStyle}"
                                        ToolTip="Zoom In (Top)" Padding="0"
                                        Click="BtnTopZoomIn_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <!-- Top DataGrid -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <DataGrid Name="TopDataGrid" Grid.Column="0" Grid.Row="0"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              SelectionMode="Single" GotFocus="TopDataGrid_GotFocus"
                              SelectionUnit="FullRow" HeadersVisibility="Column"
                              SelectionChanged="TopDataGrid_SelectionChanged"
                              PreviewKeyDown="TopDataGrid_PreviewKeyDown"
                              MouseDoubleClick="TopDataGrid_MouseDoubleClick"
                              ScrollViewer.ScrollChanged="TopDataGrid_ScrollChanged"
                              CellEditEnding="TopDataGrid_CellEditEnding"
                              Sorting="DataGrid_Sorting"
                              Background="{DynamicResource GridRowBrush}"
                              Foreground="{DynamicResource PrimaryTextBrush}"
                              BorderThickness="0" GridLinesVisibility="None"
                              RowHeaderWidth="0" RowHeight="28"
                              CanUserResizeRows="True" CanUserResizeColumns="True"
                              CanUserReorderColumns="True"
                              EnableRowVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              RowDetailsVisibilityMode="Collapsed"
                              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle}"
                              RowStyle="{DynamicResource DataGridRowStyle}"
                              CellStyle="{DynamicResource DataGridCellStyle}">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"                  Color="Transparent"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent"/>
                            </DataGrid.Resources>
                            <DataGrid.ContextMenu>
                                <ContextMenu Name="TopGridContextMenu" Opened="TopCtx_Opened">
                                    <MenuItem Header="Add to Collection (Non-Foil)"  Name="TopCtxAddToCollNonFoil"  Click="TopCtxAddToCollNonFoil_Click"/>
                                    <MenuItem Header="Add to Collection (Foil)"      Name="TopCtxAddToCollFoil"     Click="TopCtxAddToCollFoil_Click"/>
                                    <Separator Name="TopCtxDeckSeparator"/>
                                    <MenuItem Header="Add to Deck (Non-Foil)"        Name="TopCtxAddToDeckNonFoil"  Click="TopCtxAddToDeckNonFoil_Click"/>
                                    <MenuItem Header="Add to Deck (Foil)"            Name="TopCtxAddToDeckFoil"     Click="TopCtxAddToDeckFoil_Click"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,1"
                Padding="32,6,8,6">
                                        <StackPanel>
                                            <TextBlock Text="USED IN DECKS"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="#666666"
                           Margin="0,0,0,4"/>
                                            <DataGrid Name="DeckUsageGrid"
                          ItemsSource="{Binding DeckUsageRows}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          BorderThickness="1"
                          BorderBrush="{DynamicResource BorderBrush}"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          RowHeight="24"
                          MaxHeight="160"
                          CanUserResizeColumns="True"
                          CanUserReorderColumns="False"
                          Background="{DynamicResource WindowBackgroundBrush}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="Deck Name" Binding="{Binding DeckName}" Width="200"/>
                                                    <DataGridTextColumn Header="Type"      Binding="{Binding DeckType}" Width="90"/>
                                                    <DataGridTextColumn Header="Qty"       Binding="{Binding Quantity}" Width="45"/>
                                                    <DataGridTextColumn Header="Category"  Binding="{Binding Category}" Width="90"/>
                                                    <DataGridTextColumn Header="Foil"      Binding="{Binding IsFoil}"   Width="50"/>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="ES" Width="32" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Image Source="{Binding SetSymbolPath, Converter={StaticResource ImageSourceConverter}}"
                                               Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <!-- Legal: S M P L V coloured pills -->
                                <DataGridTemplateColumn Header="Legal" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="2,0">
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1"
                                                    Background="{Binding IsLegalStandard, Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="S" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1"
                                                    Background="{Binding IsLegalModern, Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="M" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1"
                                                    Background="{Binding IsLegalPioneer, Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="P" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1"
                                                    Background="{Binding IsLegalLegacy, Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="L" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1"
                                                    Background="{Binding IsLegalVintage, Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="V" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Name"         Binding="{Binding Name}"            Width="200"/>
                                <DataGridTextColumn Header="Foil"         Binding="{Binding IsFoil}"          Width="40"/>
                                <DataGridTextColumn Header="Non-Foil"     Binding="{Binding IsNonFoil}"       Width="60"/>
                                <DataGridTextColumn Header="Edition"      Binding="{Binding SetCode}"         Width="55"/>
                                <DataGridTextColumn Header="Edition Name" Binding="{Binding SetName}"         Width="160"/>
                                <DataGridTextColumn Header="Color"        Binding="{Binding ColorDisplay}"    Width="50"/>
                                <DataGridTextColumn Header="Type"         Binding="{Binding TypeLine}"        Width="180"/>
                                <DataGridTextColumn Header="Rarity"       Binding="{Binding RarityCode}"      Width="50"/>
                                <DataGridTemplateColumn Header="Cost" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ContentPresenter Content="{Binding ManaCost, Converter={StaticResource ManaCostConverter}}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="P/T"          Binding="{Binding PowerToughness}"  Width="55"/>
                                <DataGridTextColumn Header="Text"         Binding="{Binding OracleText}"      Width="220"/>
                                <DataGridTextColumn Header="Flavor"       Binding="{Binding FlavorText}"      Width="160"/>
                                <DataGridTextColumn Header="Artist"       Binding="{Binding Artist}"          Width="130"/>
                                <DataGridTextColumn Header="No"           Binding="{Binding CollectorNumber}" Width="55"/>
                                <DataGridTextColumn Header="Power"        Binding="{Binding Power}"           Width="50"/>
                                <DataGridTextColumn Header="Toughness"    Binding="{Binding Toughness}"       Width="70"/>
                                <DataGridTextColumn Header="CMC"          Binding="{Binding ManaValue}"       Width="45"/>
                                <DataGridTextColumn Header="Row"          Binding="{Binding RowIndex}"        Width="45"/>
                            </DataGrid.Columns>
                        </DataGrid>

                    </Grid>
                    <!-- Top summary grid (frozen, no headers, column-aligned totals) -->
                    <DataGrid Name="TopSummaryGrid" Grid.Row="3"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              Visibility="Collapsed"
                              CanUserAddRows="False" CanUserDeleteRows="False"
                              HeadersVisibility="None"
                              GridLinesVisibility="Horizontal"
                              RowHeight="26"
                              BorderThickness="1"
                              BorderBrush="{DynamicResource BorderBrush}"
                              Background="{DynamicResource SurfaceAltBrush}"
                              Foreground="{DynamicResource PrimaryTextBrush}"
                              FontWeight="SemiBold" FontSize="12"
                              ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                              ScrollViewer.VerticalScrollBarVisibility="Disabled"
                              CanUserResizeColumns="False"
                              CanUserReorderColumns="False"
                              Loaded="TopSummaryGrid_Loaded"/>

                    <!-- Top filter bar -->
                    <Border Grid.Row="4" Style="{DynamicResource FilterBarStyle}">
                        <DockPanel>
                            <Button Name="BtnTopFilterCustomize" Content="Customize"
                                    Style="{DynamicResource BaseButtonStyle}"
                                    Height="20" FontSize="11" Padding="6,1"
                                    DockPanel.Dock="Right" Margin="4,0"
                                    Click="BtnTopFilterCustomize_Click"/>
                            <Button Name="BtnTopFilterClear" Content="Clear"
                                    Style="{DynamicResource BaseButtonStyle}"
                                    Height="20" FontSize="11" Padding="6,1"
                                    DockPanel.Dock="Right" Margin="0,0,2,0"
                                    Click="BtnTopFilterClear_Click"/>
                            <CheckBox Name="ChkTopFilter" IsChecked="False"
                                      VerticalAlignment="Center" Margin="0,0,6,0"
                                      DockPanel.Dock="Left"
                                      Checked="ChkTopFilter_Changed"
                                      Unchecked="ChkTopFilter_Changed"/>
                            <TextBlock Name="TopFilterSummary" Text="No filter active"
                                       Foreground="{DynamicResource MutedTextBrush}"
                                       FontSize="12" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                        </DockPanel>
                    </Border>

                </Grid>

                <!-- TOP TABLE SPLITTER -->
                <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              ResizeDirection="Rows"
                              Background="{DynamicResource BorderBrush}"
                              Cursor="SizeNS"
                              Focusable="False" IsTabStop="False"/>

                <!-- BOTTOM AREA -->
                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>

                    <!-- Bottom label bar -->
                    <Border Grid.Row="0" Style="{DynamicResource GroupByBarStyle}">
                        <DockPanel Margin="6,0">
                            <Button DockPanel.Dock="Left"
                                    Content="&#x2630;"
                                    ToolTip="Show/Hide Columns"
                                    Tag="Bottom"
                                    Width="22" Height="22"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    FontSize="11"
                                    Padding="0"
                                    Margin="0,0,4,0"
                                    Style="{DynamicResource BaseButtonStyle}"
                                    PreviewMouseLeftButtonDown="ChooserBtn_PreviewMouseDown"/>
                            <StackPanel Orientation="Horizontal"
                                        VerticalAlignment="Center"
                                        DockPanel.Dock="Left">
                                <TextBlock Name="BottomTableLabel" Text="Collection"
                                           Foreground="{DynamicResource PrimaryTextBrush}"
                                           FontWeight="SemiBold" FontSize="13"
                                           VerticalAlignment="Center"/>
                                <StackPanel Name="BottomTableManaSymbols"
                                            Orientation="Horizontal"
                                            Margin="6,0,0,0"
                                            VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Bottom table zoom -->
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right"
                                        VerticalAlignment="Center" Margin="8,0,0,0">
                                <Button Content="−" Width="22" Height="22" FontSize="14"
                                        Style="{DynamicResource BaseButtonStyle}"
                                        ToolTip="Zoom Out (Bottom)" Padding="0"
                                        Click="BtnBottomZoomOut_Click"/>
                                <TextBox Name="BottomZoomInput" Text="100"
                                         Foreground="{DynamicResource MutedTextBrush}"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         FontSize="11" VerticalAlignment="Center"
                                         HorizontalContentAlignment="Center"
                                         Margin="4,0" Width="32" Height="20"
                                         PreviewKeyDown="ZoomInput_PreviewKeyDown"
                                         LostFocus="BottomZoomInput_LostFocus"
                                         GotFocus="ZoomInput_GotFocus"/>
                                <TextBlock Text="%" FontSize="11"
                                           Foreground="{DynamicResource MutedTextBrush}"
                                           VerticalAlignment="Center" Margin="0,0,2,0"/>
                                <Button Content="+" Width="22" Height="22" FontSize="14"
                                        Style="{DynamicResource BaseButtonStyle}"
                                        ToolTip="Zoom In (Bottom)" Padding="0"
                                        Click="BtnBottomZoomIn_Click"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <!-- COLLECTION DataGrid -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <DataGrid Name="BottomDataGrid" Grid.Column="0" Grid.Row="0"
                              AutoGenerateColumns="False" IsReadOnly="False"
                              SelectionMode="Single" GotFocus="BottomDataGrid_GotFocus"
                              SelectionUnit="FullRow" HeadersVisibility="Column"
                              SelectionChanged="BottomDataGrid_SelectionChanged"
                              CellEditEnding="BottomDataGrid_CellEditEnding"
                              ScrollViewer.ScrollChanged="BottomDataGrid_ScrollChanged"
                              PreviewKeyDown="BottomDataGrid_PreviewKeyDown"
                              Loaded="BottomDataGrid_Loaded"
                              Sorting="DataGrid_Sorting"
                              Background="{DynamicResource GridRowBrush}"
                              Foreground="{DynamicResource PrimaryTextBrush}"
                              BorderThickness="0" GridLinesVisibility="None"
                              RowHeaderWidth="0" RowHeight="28"
                              CanUserResizeRows="True" CanUserResizeColumns="True"
                              CanUserReorderColumns="True"
                              EnableRowVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                              RowDetailsVisibilityMode="Collapsed"
                              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle}"
                              RowStyle="{DynamicResource DataGridRowStyle}"
                              CellStyle="{DynamicResource DataGridCellStyle}">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"                  Color="Transparent"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent"/>
                            </DataGrid.Resources>
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Remove 1 Non-Foil"        Click="CtxRemove1NonFoil_Click"/>
                                    <MenuItem Header="Remove 1 Foil"            Click="CtxRemove1Foil_Click"/>
                                    <Separator/>
                                    <MenuItem Header="Remove All Copies"        Click="CtxRemoveAll_Click"/>
                                    <Separator/>
                                    <MenuItem Header="Show Deck Usage"          Click="CtxShowDeckUsage_Click"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="0,1,0,1"
                                    Padding="32,6,8,6">
                                        <StackPanel>
                                            <TextBlock Text="USED IN DECKS"
                                        FontSize="11"
                                        FontWeight="Bold"
                                        Foreground="#666666"
                                        Margin="0,0,0,4"/>
                                            <DataGrid Name="DeckUsageGrid"
                          ItemsSource="{Binding DeckUsageRows}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          BorderThickness="1"
                          BorderBrush="{DynamicResource BorderBrush}"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          RowHeight="24"
                          MaxHeight="160"
                          CanUserResizeColumns="True"
                          CanUserReorderColumns="False"
                          Background="{DynamicResource WindowBackgroundBrush}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="Deck Name" Binding="{Binding DeckName}" Width="200"/>
                                                    <DataGridTextColumn Header="Type"      Binding="{Binding DeckType}" Width="90"/>
                                                    <DataGridTextColumn Header="Qty"       Binding="{Binding Quantity}" Width="45"/>
                                                    <DataGridTextColumn Header="Category"  Binding="{Binding Category}" Width="90"/>
                                                    <DataGridTextColumn Header="Foil"      Binding="{Binding IsFoil}"   Width="50"/>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="↕" Width="28" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding ExpandGlyph}" Visibility="{Binding ExpandButtonVisibility}"
                                                Width="20" Height="20" FontSize="14" FontWeight="Bold"
                                                Padding="0" Background="Transparent" BorderThickness="0"
                                                Cursor="Hand" Tag="{Binding}" Click="BtnExpandDeckUsage_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="ES" Width="32" CanUserResize="False">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Image Source="{Binding SetSymbolPath, Converter={StaticResource ImageSourceConverter}}"
                                               Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Name"          Binding="{Binding Name}"              Width="200" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Edition"       Binding="{Binding SetCode}"           Width="55"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Edition Name"  Binding="{Binding SetName}"           Width="160" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Qty"           Binding="{Binding Quantity}"          Width="45"/>
                                <DataGridTextColumn Header="Foil Qty"      Binding="{Binding FoilQuantity}"      Width="60"/>
                                <DataGridTextColumn Header="Used"          Binding="{Binding UsedCount}"         Width="50"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Available"     Binding="{Binding AvailableCount}"    Width="70"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Buy At"        Binding="{Binding BuyAtDisplay}"             Width="70"/>
                                <DataGridTextColumn Header="Sell At"       Binding="{Binding SellAtDisplay}"            Width="70"/>
                                <DataGridTextColumn Header="Sell At Value" Binding="{Binding SellAtValueDisplay}"       Width="90"/>
                                <DataGridTextColumn Header="Price High"    Binding="{Binding PriceHighDisplay}"         Width="80"/>
                                <DataGridTextColumn Header="Foil USD"    Binding="{Binding FoilValueDisplay}"         Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Total Value"   Binding="{Binding TotalValueDisplay}"        Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Needed"        Binding="{Binding Needed}"            Width="60"/>
                                <DataGridTextColumn Header="Excess"        Binding="{Binding Excess}"            Width="60"/>
                                <DataGridTextColumn Header="Target"        Binding="{Binding Target}"            Width="60"/>
                                <DataGridTemplateColumn Header="Condition" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Condition}" VerticalAlignment="Center" Margin="4,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox Text="{Binding Condition, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center"
                                                      Background="{DynamicResource ComboBoxBackgroundBrush}" Foreground="{DynamicResource ComboBoxForegroundBrush}"
                                                      BorderBrush="#CCCCCC" BorderThickness="1"
                                                      FontSize="13" Height="28">
                                                <ComboBox.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="#FFFFFF"/>
                                                        <Setter Property="Foreground" Value="#1A1A1A"/>
                                                        <Setter Property="FontSize"   Value="13"/>
                                                        <Setter Property="Padding"    Value="6,3"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Resources>
                                                <ComboBoxItem Content="Near Mint"/>
                                                <ComboBoxItem Content="Lightly Played"/>
                                                <ComboBoxItem Content="Moderately Played"/>
                                                <ComboBoxItem Content="Heavily Played"/>
                                                <ComboBoxItem Content="Damaged"/>
                                                <ComboBoxItem Content="Unknown"/>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Language" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Language}" VerticalAlignment="Center" Margin="4,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox Text="{Binding Language, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center"
                                                      Background="{DynamicResource ComboBoxBackgroundBrush}" Foreground="{DynamicResource ComboBoxForegroundBrush}"
                                                      BorderBrush="#CCCCCC" BorderThickness="1"
                                                      FontSize="13" Height="28">
                                                <ComboBox.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="#FFFFFF"/>
                                                        <Setter Property="Foreground" Value="#1A1A1A"/>
                                                        <Setter Property="FontSize"   Value="13"/>
                                                        <Setter Property="Padding"    Value="6,3"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Resources>
                                                <ComboBoxItem Content="English"/>
                                                <ComboBoxItem Content="Japanese"/>
                                                <ComboBoxItem Content="German"/>
                                                <ComboBoxItem Content="French"/>
                                                <ComboBoxItem Content="Italian"/>
                                                <ComboBoxItem Content="Spanish"/>
                                                <ComboBoxItem Content="Portuguese"/>
                                                <ComboBoxItem Content="Russian"/>
                                                <ComboBoxItem Content="Korean"/>
                                                <ComboBoxItem Content="Chinese Simplified"/>
                                                <ComboBoxItem Content="Chinese Traditional"/>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Notes"         Binding="{Binding Notes}"             Width="150"/>
                                <DataGridTextColumn Header="Storage"       Binding="{Binding StorageLocation}"   Width="100"/>
                                <DataGridTemplateColumn Header="Desired" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Desired}" VerticalAlignment="Center" Margin="4,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox Text="{Binding Desired, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center"
                                                      Background="{DynamicResource ComboBoxBackgroundBrush}" Foreground="{DynamicResource ComboBoxForegroundBrush}"
                                                      BorderBrush="#CCCCCC" BorderThickness="1"
                                                      FontSize="13" Height="28">
                                                <ComboBox.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="#FFFFFF"/>
                                                        <Setter Property="Foreground" Value="#1A1A1A"/>
                                                        <Setter Property="FontSize"   Value="13"/>
                                                        <Setter Property="Padding"    Value="6,3"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Resources>
                                                <ComboBoxItem Content="Unassigned"/>
                                                <ComboBoxItem Content="Junk"/>
                                                <ComboBoxItem Content="Needed"/>
                                                <ComboBoxItem Content="Needed Badly"/>
                                                <ComboBoxItem Content="Normal"/>
                                                <ComboBoxItem Content="Not Needed"/>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Group"         Binding="{Binding CardGroup}"         Width="80"/>
                                <DataGridTemplateColumn Header="Print Type" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding PrintType}" VerticalAlignment="Center" Margin="4,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox Text="{Binding PrintType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center"
                                                      Background="{DynamicResource ComboBoxBackgroundBrush}" Foreground="{DynamicResource ComboBoxForegroundBrush}"
                                                      BorderBrush="#CCCCCC" BorderThickness="1"
                                                      FontSize="13" Height="28">
                                                <ComboBox.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="#FFFFFF"/>
                                                        <Setter Property="Foreground" Value="#1A1A1A"/>
                                                        <Setter Property="FontSize"   Value="13"/>
                                                        <Setter Property="Padding"    Value="6,3"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Resources>
                                                <ComboBoxItem Content="Unknown"/>
                                                <ComboBoxItem Content="Duels"/>
                                                <ComboBoxItem Content="MTGA"/>
                                                <ComboBoxItem Content="MTGO"/>
                                                <ComboBoxItem Content="Paper"/>
                                                <ComboBoxItem Content="Proxy"/>
                                                <ComboBoxItem Content="Virtual"/>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Buy" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding BuyStatus}" VerticalAlignment="Center" Margin="4,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox Text="{Binding BuyStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center"
                                                      Background="{DynamicResource ComboBoxBackgroundBrush}" Foreground="{DynamicResource ComboBoxForegroundBrush}"
                                                      BorderBrush="#CCCCCC" BorderThickness="1"
                                                      FontSize="13" Height="28">
                                                <ComboBox.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="#FFFFFF"/>
                                                        <Setter Property="Foreground" Value="#1A1A1A"/>
                                                        <Setter Property="FontSize"   Value="13"/>
                                                        <Setter Property="Padding"    Value="6,3"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Resources>
                                                <ComboBoxItem Content="Unassigned"/>
                                                <ComboBoxItem Content="None"/>
                                                <ComboBoxItem Content="Buy"/>
                                                <ComboBoxItem Content="Exchange"/>
                                                <ComboBoxItem Content="B&amp;E"/>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Sell" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding SellStatus}" VerticalAlignment="Center" Margin="4,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox Text="{Binding SellStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      IsEditable="False"
                                                      VerticalAlignment="Center"
                                                      Background="{DynamicResource ComboBoxBackgroundBrush}" Foreground="{DynamicResource ComboBoxForegroundBrush}"
                                                      BorderBrush="#CCCCCC" BorderThickness="1"
                                                      FontSize="13" Height="28">
                                                <ComboBox.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="#FFFFFF"/>
                                                        <Setter Property="Foreground" Value="#1A1A1A"/>
                                                        <Setter Property="FontSize"   Value="13"/>
                                                        <Setter Property="Padding"    Value="6,3"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#0078D4"/>
                                                                <Setter Property="Foreground" Value="#1A1A1A"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Resources>
                                                <ComboBoxItem Content="Unassigned"/>
                                                <ComboBoxItem Content="None"/>
                                                <ComboBoxItem Content="Buy"/>
                                                <ComboBoxItem Content="Exchange"/>
                                                <ComboBoxItem Content="B&amp;E"/>
                                            </ComboBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Added"         Binding="{Binding DateAdded, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="130" IsReadOnly="True"/>
                                <DataGridTextColumn Header="USD"   Binding="{Binding PriceUsdDisplay}"  Width="90"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Price Low"      Binding="{Binding PriceLowDisplay}"  Width="80"  IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Legal" Width="100" IsReadOnly="True">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="2,0">
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1" Background="{Binding IsLegalStandard, Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="S" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1" Background="{Binding IsLegalModern,   Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="M" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1" Background="{Binding IsLegalPioneer,  Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="P" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1" Background="{Binding IsLegalLegacy,   Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="L" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                                <Border CornerRadius="3" Margin="1,0" Padding="3,1" Background="{Binding IsLegalVintage,  Converter={StaticResource BoolToGreenRedConverter}}">
                                                    <TextBlock Text="V" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                                </Border>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Color"         Binding="{Binding ColorDisplay}"      Width="50"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Type"          Binding="{Binding TypeLine}"          Width="160" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Rarity"        Binding="{Binding RarityCode}"        Width="50"  IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Cost" Width="110" IsReadOnly="True">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ContentPresenter Content="{Binding ManaCost, Converter={StaticResource ManaCostConverter}}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="P/T"           Binding="{Binding PowerToughness}"    Width="55"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Text"          Binding="{Binding OracleText}"        Width="220" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Flavor"        Binding="{Binding FlavorText}"        Width="160" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Artist"        Binding="{Binding Artist}"            Width="130" IsReadOnly="True"/>
                                <DataGridTextColumn Header="No"            Binding="{Binding CollectorNumber}"   Width="55"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Power"         Binding="{Binding Power}"             Width="50"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Toughness"     Binding="{Binding Toughness}"         Width="70"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="CMC"           Binding="{Binding ManaValue}"         Width="45"  IsReadOnly="True"/>
                                <DataGridTextColumn Header="Row"           Binding="{Binding RowIndex}"          Width="45"  IsReadOnly="True"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>



                    <!-- Bottom summary grid (frozen, no headers, column-aligned totals) -->
                    <DataGrid Name="BottomSummaryGrid" Grid.Row="2"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" CanUserDeleteRows="False"
                              HeadersVisibility="None"
                              GridLinesVisibility="Horizontal"
                              RowHeight="26"
                              BorderThickness="1"
                              BorderBrush="{DynamicResource BorderBrush}"
                              Background="{DynamicResource SurfaceAltBrush}"
                              Foreground="{DynamicResource PrimaryTextBrush}"
                              FontWeight="SemiBold" FontSize="12"
                              ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                              ScrollViewer.VerticalScrollBarVisibility="Disabled"
                              CanUserResizeColumns="False"
                              CanUserReorderColumns="False"
                              Loaded="BottomSummaryGrid_Loaded"/>

                    <!-- Bottom filter bar -->
                    <Border Grid.Row="3" Style="{DynamicResource FilterBarStyle}">
                        <DockPanel>
                            <Button Name="BtnBottomFilterCustomize" Content="Customize"
                                    Style="{DynamicResource BaseButtonStyle}"
                                    Height="20" FontSize="11" Padding="6,1"
                                    DockPanel.Dock="Right" Margin="4,0"
                                    Click="BtnBottomFilterCustomize_Click"/>
                            <Button Name="BtnBottomFilterClear" Content="Clear"
                                    Style="{DynamicResource BaseButtonStyle}"
                                    Height="20" FontSize="11" Padding="6,1"
                                    DockPanel.Dock="Right" Margin="0,0,2,0"
                                    Click="BtnBottomFilterClear_Click"/>
                            <CheckBox Name="ChkBottomFilter" IsChecked="False"
                                      VerticalAlignment="Center" Margin="0,0,6,0"
                                      DockPanel.Dock="Left"
                                      Checked="ChkBottomFilter_Changed"
                                      Unchecked="ChkBottomFilter_Changed"/>
                            <TextBlock Name="BottomFilterSummary" Text="No filter active"
                                       Foreground="{DynamicResource MutedTextBrush}"
                                       FontSize="12" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                        </DockPanel>
                    </Border>

                </Grid>

                <!-- DECK TABS — permanent strip at bottom of right panel -->
                <TabControl Name="DeckTabControl" Grid.Row="3"
                            TabStripPlacement="Bottom"
                            Visibility="Collapsed"
                            Height="30"
                            Background="{DynamicResource SurfaceAltBrush}"
                            BorderThickness="0,1,0,0"
                            BorderBrush="{DynamicResource BorderBrush}"
                            Padding="0"
                            SelectionChanged="DeckTabControl_SelectionChanged">
                </TabControl>

            </Grid>
        </Grid>

        <!-- STATUS BAR -->
        <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0" Height="22">
            <DockPanel Margin="8,0">
                <TextBlock Name="StatusText" Text="Ready"
                           Foreground="{DynamicResource MutedTextBrush}"
                           FontSize="11" VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>
                <ProgressBar Name="StatusProgressBar"
                             DockPanel.Dock="Right"
                             Width="150" Height="12"
                             Minimum="0" Maximum="100"
                             Visibility="Collapsed"
                             Margin="8,0,0,0"/>
            </DockPanel>
        </Border>

    </Grid>
</Window>
